cmake -B build

cmake --build build --config Release      
                                                               
double-cliquer sur le .exe (ne pas oublier de copier-coller les deux dlls dans le dossier build.)
